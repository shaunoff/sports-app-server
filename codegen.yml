overwrite: true
schema: 'src/typeDefs/**/*.ts'
generates:
  generated/graphql.ts:
    hooks:
      afterAllFileWrite:
        - nodemon
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
  generated/graphql.schema.json:
    plugins:
      - 'introspection'
